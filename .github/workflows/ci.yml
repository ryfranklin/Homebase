name: ci

# Continuous integration for Homebase. Scans, formatting, lint, and tests, plus a
# CONTENT-ONLY web deploy on push to main (S3 sync + CloudFront invalidation).
# Infrastructure (terraform apply) and the agent image are NEVER deployed here:
# those remain human actions (see scripts/deploy-agent.sh and the RUNBOOK). The web
# deploy authenticates with GitHub OIDC (no long-lived keys) and is skipped until
# the deploy vars/secret are configured (see infra/stacks/cicd).

on:
  pull_request:
  push:
    branches: ["**"]

permissions:
  contents: read

jobs:
  secret-scan:
    name: gitleaks secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          # Fail the job on any finding.
          GITLEAKS_ENABLE_UPLOAD_ARTIFACT: "false"

  terraform-fmt:
    name: terraform fmt check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false
      - name: terraform fmt -check
        run: |
          if [ -d infra ] && find infra -name '*.tf' -print -quit | grep -q .; then
            terraform fmt -check -recursive infra
          else
            echo "No Terraform files yet; skipping fmt check."
          fi

  lint:
    name: language linters (where code exists)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Lint web and cli (if configured)
        run: |
          for dir in web cli; do
            if [ -f "$dir/package.json" ]; then
              echo "Linting $dir"
              (cd "$dir" && npm ci && npm run lint --if-present)
            else
              echo "No package.json in $dir; skipping."
            fi
          done

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Lint Python (if configured)
        run: |
          if find services eval cli -name '*.py' -print -quit 2>/dev/null | grep -q .; then
            python -m pip install --upgrade pip ruff
            ruff check services eval cli || true
          else
            echo "No Python files yet; skipping."
          fi

  tests:
    name: unit tests (offline, no cloud credentials)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Python unit tests
        run: |
          for pkg in services/ingestion services/agent services/connectors cli eval; do
            if [ -d "$pkg/tests" ]; then
              echo "== $pkg =="
              (cd "$pkg" && python -m unittest discover -s tests)
            fi
          done
      - name: BFF unit tests
        run: |
          if [ -f services/bff/package.json ]; then (cd services/bff && node --test); fi

  eval-gate:
    name: retrieval eval gate (offline regression gate)
    runs-on: ubuntu-latest
    # This gate runs OFFLINE against committed synthetic fixtures with NO cloud
    # credentials. It guards regressions in the retrieval CODE, not absolute
    # quality on the real corpus (that is the separate live ADR-002 eval). See
    # docs/eval-gate.md. It must never be given AWS credentials or secrets.
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run the retrieval regression gate
        run: |
          cd eval
          PYTHONPATH=src python -m homebase_eval.cli --gate --k 3

  deploy-web:
    name: deploy web (S3 sync + CloudFront invalidation)
    # CONTENT deploy only. This never runs terraform apply and never touches the
    # agent image (both remain human actions). It runs only on a push to main,
    # only after the checks pass, and only once the deploy vars are configured
    # (vars.WEB_BUCKET). Auth is GitHub OIDC, so there are NO long-lived AWS keys.
    # All account-specific values come from repo Variables/Secrets, never literals.
    needs: [secret-scan, terraform-fmt, lint, tests, eval-gate]
    if: >-
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      vars.WEB_BUCKET != ''
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for OIDC role assumption
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Configure AWS credentials (GitHub OIDC, no static keys)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Build the SPA
        working-directory: web
        # Vite build-time config. These are public client identifiers (they ship in
        # the bundle anyway), injected from repo Variables so nothing is hardcoded.
        env:
          VITE_COGNITO_USER_POOL_ID: ${{ vars.VITE_COGNITO_USER_POOL_ID }}
          VITE_COGNITO_CLIENT_ID: ${{ vars.VITE_COGNITO_CLIENT_ID }}
          VITE_COGNITO_HOSTED_UI_DOMAIN: ${{ vars.VITE_COGNITO_HOSTED_UI_DOMAIN }}
          VITE_COGNITO_SCOPES: ${{ vars.VITE_COGNITO_SCOPES }}
          VITE_REDIRECT_URI: ${{ vars.VITE_REDIRECT_URI }}
          VITE_LOGOUT_URI: ${{ vars.VITE_LOGOUT_URI }}
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
        run: |
          npm ci
          npm run build

      - name: Sync to S3 and invalidate CloudFront
        run: |
          aws s3 sync web/dist "s3://${{ vars.WEB_BUCKET }}/" --delete
          aws cloudfront create-invalidation \
            --distribution-id "${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}" \
            --paths "/*"
