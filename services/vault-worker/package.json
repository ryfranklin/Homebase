{
  "name": "homebase-vault-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Always-on worker that owns the git vault clone and mirrors git -> S3 -> Knowledge Base.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "tsx": "^4.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
